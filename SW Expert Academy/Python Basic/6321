def PrimeNumber(num):
    count=0
    for i in range(2,num):
        if num%i == 0:
            count+=1
    if count!=0:    
        print("소수가 아닙니다.")
    else:
        print("소수입니다.")
            
num=int(input())
PrimeNumber(num) 
