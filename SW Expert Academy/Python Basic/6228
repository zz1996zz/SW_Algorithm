class Shape:
    def area(self):
        print(0)
class Square(Shape):
    def __init__(self,length):
        self.length=length
    def area(self):
        print("정사각형의 면적: {0}" .format(self.length*self.length))
a=Square(3)
a.area()
